node_modules
dist
.env
.env.local
.DS_Store
*.log
.claude/settings.local.json

# Secrets / credentials living loose in docs (use .env.local instead)
docs/Stripe.txt
docs/*-keys.*
docs/*-secret*.*

# Nested marketing-site repo (lives in its own git remote)
noworry-home-site/
